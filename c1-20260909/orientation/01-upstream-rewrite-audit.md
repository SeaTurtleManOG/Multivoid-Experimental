# SUMMARY
All ten R3P player-log candidates were re-verified against both upstream heads: every cited defect site still exists at new upstream main 3af5ddae, with only comment sweeps and line-number drift — no candidate is superseded by the upstream rewrite, and the three shipped fixes (10777571, 46b77b92, 15589b7f) are absent from 3af5ddae, so they still apply. The binding analysis review (ws-player-logs-review-codex/MATRIX.md) determined 4 SOURCE_PROVEN (1, 2, 8, 10) and 6 SOURCE_CONSISTENT (3, 4, 5, 6, 7, 9), authorised implementation only for 1, 2, 8, 10, and the R3Q author lane deliberately deferred 8 because it correlates with BUG-017 whose fix is gated on a runtime packet that was NOT_CLEAN. That leaves exactly ONE fix-ready candidate today: candidate 8, the 32-entry client placement queue that tests its cap BEFORE sampling container-extraction eligibility (prop_drop_intent.cpp:141 at both revisions, with the eligibility latch at :148). Candidates 3, 4, 5, 6, 7 and 9 are all NEEDS_RUNTIME — each is a source-consistent policy branch where the source proves the drop but not that the dropped input was legitimate, and each review conclusion explicitly withholds implementation authorisation. Candidate 6 has a partial upstream change worth knowing: 3af5ddae added coop::unresolved_pose_ledger, which demotes the per-packet "no local match" WARN to INFO and raises a WARN only for a sustained stream, so the log signature that produced the 214 counts no longer exists in the same form on new main, while the drop-without-apply mechanism is unchanged. Beyond the ten, the stage records four other unfixed items: the SOURCE_PROVEN baseline delivery-invariant defect in interactable_channel.h (a fix exists at 1c393401, reviewed BASELINE_FIX_CLEAN, unpublished, latent until someone shows SendReliableToSlot can fail), the two MEASURED R3Q observations OBS-R3Q-C01 (zero-eid keyed DESTROY over-matching and killing a just-placed mirror on the client) and OBS-R3Q-P01 (lid-state divergence), and the four published findings MVF-0001..0004, none of which has a fix. The single highest-value new work is the destroy-side counterpart to the already-fixed candidate 2: OBS-R3Q-C01 is MEASURED on both peers, has no fix, and the destroy gate at event_dispatch_entity.cpp:236-240 (3af5ddae) is the mirror image of the spawn gate that 46b77b92 already reshaped.

# FACTS
- [SOURCE_PROVEN] CANDIDATE 1 — roach scale call proceeds with an unset parameter frame. Log signature: ERROR "ParamFrame::Set: unknown param 'NewScale3D'", 3,218 occurrences in L2 lines 55,092-118,715, 0 in L1; no ledger identity, no player report. Source position f690c017: roach_sync.cpp:156-161 (SetRaw at :160, result ignored), call.cpp:119-133 (SetRaw returns false at :124 before the memcpy at :131), call.cpp:151-154 (Call gates only on frame.valid()), call.h:27 (valid() == fn_!=nullptr). Same spot on 3af5ddae: roach_sync.cpp:156-161 IDENTICAL line numbers, call.cpp:113-127 and 145-148, call.h:27 — code still there, unchanged logic. STRENGTH SOURCE_PROVEN (reviewer and candidate agree). FALSIFIER: the failed path writes the requested bytes, the caller checks/propagates failure, Call includes write success in its gate, or CallFunction does not invoke ProcessEvent on this path. FIX EXISTS: yes, 10777571 (call.h +4, call.cpp +47, standalone test candidate_01_param_frame_boundary.cpp), reviewed PLAYER_LOG_FIXES_CLEAN, publication authorised. VERDICT: FIXED — port to 3af5ddae, no impact claim authorised from the 3,218 occurrences.
    evidence: git show 3af5ddae:src/votv-coop/src/ue_wrap/core/call.cpp lines 113-127,145-148; git show 3af5ddae:src/votv-coop/src/coop/creatures/roach_sync.cpp:156-161; C:\AgenticStaging\VOTV\Coordination\R3Q\LANES\ws-player-logs-review-codex\MATRIX.md 'Candidate 1'; ws-player-log-fix-review-codex\REVIEW.md 'Candidate 1 — 10777571'
- [SOURCE_PROVEN] CANDIDATE 2 — zero-EID PropSpawn rejected before materialization. Log signature: WARN "event_feed: PropSpawn elementId=0x00000000 out of allowed <host(any)|peer> range (senderPeerSlot=N) -- dropping", 1 in L1 (line 39,601) + 24 in L2. Source position f690c017: event_dispatch_entity.cpp:205-223 (range gate) then :246 (OnSpawn), registry.h:165-174 (both ranges exclude 0), remote_prop_spawn.cpp:152-153 (the keyed-zero acceptance branch, unreachable because rejection is 'eidOnly && elementId == 0'), four sender sites mapping kInvalidId to 0u: prop_lifecycle.cpp, prop_snapshot.cpp, prop_container_extract.cpp, trash_collect_sync.cpp. Same spot on 3af5ddae: gate at event_dispatch_entity.cpp:178-192, OnSpawn at :210, registry.h:91-100, remote_prop_spawn.cpp:136-137 — code still there, predicates byte-identical. STRENGTH SOURCE_PROVEN. FALSIFIER: any named sender does not encode invalid EID as zero, either allocation predicate accepts zero, the dispatcher calls OnSpawn before the gate, or OnSpawn rejects keyed zero unconditionally. FIX EXISTS: yes, 46b77b92 (new include/coop/dispatch/prop_spawn_gate.h, event_dispatch_entity.cpp hunk, key printed in the refusal warning, standalone test). VERDICT: FIXED — but see OBS-R3Q-C01: the DESTROY side is a separate, still-unfixed defect and must not be conflated.
    evidence: git show 3af5ddae:src/votv-coop/src/coop/dispatch/event_dispatch_entity.cpp:178-192,210; git show 3af5ddae:src/votv-coop/src/coop/props/remote_prop_spawn.cpp:136-137; git show 3af5ddae:src/votv-coop/include/coop/element/registry.h:91-100; git show --stat 46b77b92
- [INFERRED] CANDIDATE 3 — an unbound pile consumes and cancels the client's grab press. Log signature: WARN "[GRAB-INTENT] CLIENT E-PRESS on UNBOUND native pile %p POST-quiescence -- identity-less ghost -> arming the wholesale GHOST-RETIRE reconcile", 17 in L2 (lines 40,996-44,585), 0 in L1. Source position f690c017: trash_use_intercept.cpp:212-234 (WARN at :229, ArmGhostSweep at :232, paired-release latch at :233). Same spot on 3af5ddae: trash_use_intercept.cpp:188-201 (WARN at :196) — code still there. STRENGTH INFERRED (reviewer label SOURCE_CONSISTENT). FALSIFIER: the pinned branch lets the native interaction proceed, or actor-identity telemetry proves every warned actor was validly bound at the press. FIX EXISTS: no. VERDICT: NEEDS_RUNTIME — IMPLEMENTATION_AUTHORISED NO; the evidence cannot distinguish intended ghost protection from a live identity defect, and no correct fix shape can be chosen without a rig run that shows a visible legitimate pile with no EID.
    evidence: git show 3af5ddae:src/votv-coop/src/coop/props/trash_use_intercept.cpp:188-201; MATRIX.md 'Candidate 3 — unbound pile E-press'
- [INFERRED] CANDIDATE 4 — remote puppet render position trails targets by over 5 m. Log signature: WARN "pose-diag[slot %d]: puppet TRAILS its target by %.0f cm (fresh=%d/s) -- the apply/interp is behind the on-time pose stream", 1 in L1 (line 54,467) + 30 in L2. Source position f690c017: puppet_drive.cpp:191-209 (trail computed at :201, threshold 500.f at :206, WARN at :207). Same spot on 3af5ddae: puppet_drive.cpp:187-205 (trail :197, threshold :202, WARN :203) — code still there. STRENGTH INFERRED (SOURCE_CONSISTENT). FALSIFIER: a complete same-input reconstruction places L2 lines 91,645 and 137,748 inside a ragdoll interval, or pinned source identifies another benign transform owner covering both. NOTE: the review's own complete state scan attributed 29 of 31 warnings to ragdoll attachment; only two records survive outside every logged ragdoll interval, so this is a near-refuted candidate that was explicitly kept rather than retired. FIX EXISTS: no. VERDICT: NEEDS_RUNTIME — paired world-space capture with synchronized clocks is required before any change.
    evidence: git show 3af5ddae:src/votv-coop/src/coop/player/puppet_drive.cpp:187-205; MATRIX.md 'Candidate 4 — puppet trail diagnostic'
- [INFERRED] CANDIDATE 5 — parked ATV mirrors leave the authority pose. Log signature: WARN "atv: a parked mirror would not stay on the authority's pose after %d re-places (last error %.1f cm). CHECK THE RIG'S SHAPE BEFORE THE WORLD...", 96 total (L1 58 from line 18,161, L2 38). Source position f690c017: atv_corrector.cpp:230-274 (WARN at :266-272, TeleportRig at :245). Same spot on 3af5ddae: atv_corrector.cpp:205-226 (WARN at :219-225, TeleportRig at :205) — code still there, message string identical. STRENGTH INFERRED (SOURCE_CONSISTENT). FALSIFIER: pinned source samples the displayed distance after the third teleport, or paired rig telemetry shows all warnings arise from a known benign geometry condition. FIX EXISTS: no. VERDICT: NEEDS_RUNTIME — the warning itself names its own discriminator ([dev] atv_probe=1, compare rideH on both peers); until that paired [ATVP] comparison exists, the wrong-shape and wrong-place classes cannot be separated and any fix would be guessing.
    evidence: git show 3af5ddae:src/votv-coop/src/coop/interactables/atv_corrector.cpp:205-226; MATRIX.md 'Candidate 5 — ATV corrective warning'
- [INFERRED] CANDIDATE 6 — held-prop poses dropped when receiver key-then-EID lookup fails. Log signature at the measured build: WARN "remote_prop: slot %d incoming PropPose key '%ls' eid=%u -- no local match (key or eid)", 214 total (L1 205, L2 9); 91 of the L1 instances are one key with eid=0. Source position f690c017: remote_prop.cpp:158-188 (key lookup, then eid lookup, WARN at :186, then bare return without applying). Same spot on 3af5ddae: remote_prop.cpp:128-173 — the drop-without-apply is unchanged at :155-173, BUT upstream added coop::unresolved_pose_ledger: a sustained stream now raises a distinct WARN at :159-166 and the ordinary case is demoted to UE_LOGI at :168-170. STRENGTH INFERRED (SOURCE_CONSISTENT). FALSIFIER: the pinned warning path applies the pose anyway, or complete actor/registry telemetry proves a single source-supported root cause for all warned events. FIX EXISTS: no. VERDICT: NEEDS_RUNTIME, with a diagnostics caveat — the exact signature this candidate was counted from is PARTLY SUPERSEDED on new main (severity split and one-per-stream rate limiting), so a re-count on 3af5ddae will not reproduce 214; the mechanism is not superseded.
    evidence: git show 3af5ddae:src/votv-coop/src/coop/props/remote_prop.cpp:155-173 vs git show f690c017:...:158-188; MATRIX.md 'Candidate 6 — PropPose no local match'
- [INFERRED] CANDIDATE 7 — deferred-spawn null counted as applied, never retried. Log signature: ERROR "remote_prop::OnSpawn: BeginDeferred returned null", 4 in L2 (lines 97,850-97,880), 0 in L1, all within ~2 s of a menu transition. Source position f690c017: prop_fresh_spawn.cpp:120-127 (null return), remote_prop_spawn.cpp:699-703 (caller leaves outSpawned untouched on null), event_dispatch_entity.cpp:246-250 (NotePropApplied advances anyway). Same spot on 3af5ddae: prop_fresh_spawn.cpp:114-121, remote_prop_spawn.cpp:529-533, event_dispatch_entity.cpp:210-212 — code still there. STRENGTH INFERRED (SOURCE_CONSISTENT). FALSIFIER: the same invocation is retried by a pinned path, or a stable-world probe shows no persistent absence after a null return. Reviewer additionally SOURCE_REFUTED the sub-claim that NotePropApplied makes the spawn non-retryable: it is a join loading-bar counter that returns immediately outside the Receiving phase (join_progress.cpp:97-124). FIX EXISTS: no. VERDICT: NEEDS_RUNTIME — all four observed instances are teardown-bound; a stable-world reproduction is the gate.
    evidence: git show 3af5ddae:src/votv-coop/src/coop/props/prop_fresh_spawn.cpp:114-121; git show 3af5ddae:src/votv-coop/src/coop/props/remote_prop_spawn.cpp:529-533; git show 3af5ddae:src/votv-coop/src/coop/dispatch/event_dispatch_entity.cpp:210-212; MATRIX.md 'Candidate 7 — deferred spawn returned null'
- [SOURCE_PROVEN] CANDIDATE 8 — client place observations dropped at a 32-entry queue cap. Log signature: WARN "[PROP-DROP] client pending-place cap 32 hit -- dropping %p", 179 in L2 (lines 16,626-141,084). Source position f690c017: prop_drop_intent.cpp:141-149 — the cap test 'g_pending.size() >= kMaxPending' is at :141 and returns at :143, while the eligibility sample 'container_contents_sync::TakeObjInFlight()' is at :148 and the enqueue at :149; drain-time eligibility lives at :262-357. Same spot on 3af5ddae: prop_drop_intent.cpp:141-149 with kMaxPending=32 at :64 — IDENTICAL line numbers, code still there. STRENGTH SOURCE_PROVEN (reviewer and candidate agree: the queue rejects an arrival at size 32 before sampling eligibility, while the drain applies eligibility, so ineligible occupants can consume all slots and an eligible arrival is refused with no retry). FALSIFIER: the pinned enqueue path evaluates the same eligibility before the cap, reserves capacity for eligible entries, evicts/retries refused eligible arrivals, or the drain has no later eligibility distinction. FIX EXISTS: no — IMPLEMENTATION_AUTHORISED YES by the binding review, but the R3Q author lane was explicitly told 'Candidate 8 is NOT in your scope' because it correlates with BUG-017 whose fix was gated on a NOT_CLEAN runtime packet. VERDICT: READY — the admission-ordering defect is source-proven, bounded to one function, and testable without the game exactly as candidates 2 and 10 were.
    evidence: git show 3af5ddae:src/votv-coop/src/coop/props/prop_drop_intent.cpp:64,141-149 (identical to f690c017); MATRIX.md 'Candidate 8 — 32-entry client placement queue cap'; ws-player-log-fix-codex\WORK_ORDER.md 'Candidate 8 is NOT in your scope'
- [SOURCE_PROVEN] CANDIDATE 8 FIX SKETCH (READY). Shape: extract the admission decision out of OnClientFinishSpawn into a header-only pure policy (mirroring include/coop/dispatch/prop_spawn_gate.h from 46b77b92 and include/coop/interactables/pending_retry.h from 15589b7f), taking (queue size, cap, eligibility flags) and returning Admit / AdmitByEviction / Refuse. Sample eligibility BEFORE the capacity test so a container-extraction birth is never refused by churn, and when the queue is full admit an eligible arrival by evicting the oldest INELIGIBLE entry rather than dropping the newcomer; refuse only when all 32 occupants are themselves eligible, and keep the 32-entry bound so nothing becomes unbounded. One trap the implementation must handle: container_contents_sync::TakeObjInFlight() is a CONSUMING latch, so it must be sampled exactly once per arrival and its value carried into the policy — never called on a path that then refuses, or a refused arrival silently eats the eligibility of the next one. Standalone test shape: src/votv-coop/tests/standalone/candidate_08_place_queue_admission.cpp — positive control (empty queue admits, eligible and ineligible alike), the killing case (fill 32 ineligible entries, then push one eligible container-extraction birth: pin refuses and exits 1, fix admits and exits 0), the inverted-order control (eligible first, then 32 ineligible — must be unaffected), a bound assertion (queue never exceeds 32 after eviction), and the omission case (restore cap-before-eligibility and the killing case fails again). Compile the single translation unit prop_drop_intent.cpp with MSVC 14.50 /std:c++20 /EHsc /W4 as the fix lane did; no game process, no protocol change.
    evidence: shape derived from git show --stat 46b77b92 and 15589b7f (prop_spawn_gate.h / pending_retry.h + tests/standalone/*), applied to prop_drop_intent.cpp:141-149; consuming latch at prop_drop_intent.cpp:148 'const bool fromContainerExtract = coop::props::container_contents_sync::TakeObjInFlight();'
- [INFERRED] CANDIDATE 9 — frozen/static classification suppresses a pose stream. Log signature: WARN "remote_prop: slot %d PropPose for frozen/static non-attachable %p -- ignored", 763 total (L1 30 for one pointer, L2 733 across five pointers: 354/144/137/73/25). Source position f690c017: remote_prop.cpp:190-214 (IsFrozen||IsStatic and not wall-attachable, WARN at :200, return). Same spot on 3af5ddae: remote_prop.cpp:179-184 (WARN at :182) — code still there, message identical. STRENGTH INFERRED (SOURCE_CONSISTENT). FALSIFIER: paired state shows every actor still frozen/static at the sender, or all poses stale after a state transition. FIX EXISTS: no. VERDICT: NEEDS_RUNTIME — the branch is correct for genuinely immutable actors; the warning exposes a deliberate policy, and nothing in the logs says whether the sender considered the actor movable, so no corrective change can be selected.
    evidence: git show 3af5ddae:src/votv-coop/src/coop/props/remote_prop.cpp:179-184; MATRIX.md 'Candidate 9 — frozen/static PropPose ignored'
- [SOURCE_PROVEN] CANDIDATE 10 — drive state expires before its actor resolves. Log signature: WARN "drive_sync: pending apply kind=%d expired (actor never resolved)", 9 in L2 (lines 14,772-137,743). Source position f690c017: drive_sync.cpp:416-452 — RetryPendingTick tests 'now >= pd.until' at :421 and discards at :422, BEFORE the LivePropActor lookups at :437 and :445; 10 s TTL, 256-entry cap, 1 Hz cadence at :515-520. Same spot on 3af5ddae: drive_sync.cpp:413-450 — deadline at :418, warning at :419, lookups at :434 and :442; code still there, ordering unchanged. STRENGTH SOURCE_PROVEN. FALSIFIER: the pinned retry loop resolves before checking the deadline, performs a final resolution at expiry, or the warning text does not claim non-resolution. FIX EXISTS: yes, 15589b7f (new include/coop/interactables/pending_retry.h, drive_sync.cpp reorder, standalone test), reviewed clean, TTL and 256-entry cap retained. VERDICT: FIXED — port to 3af5ddae; observed-instance impact remains UNKNOWN.
    evidence: git show 3af5ddae:src/votv-coop/src/coop/interactables/drive_sync.cpp:413-450; git show --stat 15589b7f; ws-player-log-fix-review-codex\REVIEW.md 'Candidate 10 — 15589b7f'
- [SOURCE_PROVEN] PORTABILITY OF THE THREE EXISTING FIXES. The three fix commits are rooted on f690c017, which the 2026-09-08 upstream force-rewrite orphaned (b150 tag ba6d8c39 and old main are not ancestors of 3af5ddae). None of their content is present on 3af5ddae: include/coop/dispatch/prop_spawn_gate.h and include/coop/interactables/pending_retry.h do not exist there, call.cpp:145-148 still gates on frame.valid() with no ready(), and drive_sync.cpp:418-419 still discards on the deadline before any lookup. All three target regions are byte-identical in logic at both revisions, so the ports are line-shift-only, not redesigns.
    evidence: git ls-tree -r --name-only 3af5ddae | grep prop_spawn_gate.h -> empty; same for pending_retry.h; git show 3af5ddae:.../call.cpp:145-148; git show 3af5ddae:.../drive_sync.cpp:418-419
- [SOURCE_PROVEN] OTHER UNFIXED #1 — the baseline delivery-invariant defect. Signature: host log "connect-snapshot -- sent N full state(s)" counting an unacknowledged send as sent; no player report attributed. Source position f690c017: include/coop/interactables/interactable_channel.h — steady poll at ~:205 banks lastKnown_ only inside 'if (s->SendReliable(...))', connect snapshot at ~:427 calls SendReliableToSlot, discards the result, banks lastKnown_ and does ++sent. STRENGTH SOURCE_PROVEN, verified twice independently (found by ws-fork-audit on Elgias/Multivoid fix/door-hold-register as C-15/C-22, confirmed by the orchestrator reading both sites in the pinned canonical tree). FALSIFIER: SendReliableToSlot cannot fail; or another guaranteed path re-sends connect-seed state after a failed send; or lastKnown_ is reset on the join path elsewhere. FIX EXISTS: yes — 1c393401 on fix/baseline-delivery-invariant-20260904, ONE commit (interactable_channel.h +143, session.{h,cpp}, interactable_sync.cpp, tests/baseline_delivery/), reviewed BASELINE_FIX_CLEAN by an independent codex lane, publication authorised, not published. VERDICT: FIXED-BUT-UNPUBLISHED, and LATENT: whether the connect-time send can actually fail is UNKNOWN and that gap must travel with the finding. The C1 brief records 143 changed lines in interactable_channel.h upstream (119 non-comment), so this port is the only one that is NOT line-shift-only.
    evidence: git show 88841428:handoffs/stages/20260903T222051Z/BASELINE-DEFECT-INDEPENDENTLY-VERIFIED.md; C:\AgenticStaging\VOTV\Coordination\C1\CONTEXT.md lines 53,65-66,35
- [MEASURED] OTHER UNFIXED #2 — OBS-R3Q-C01, the client loses a host-placed Drive box to a same-key eid=0 destroy. Signature: client "remote_prop::OnDestroy: key 'HlwQ1k8xmW4zxWDHX-VZFw' eid=0 -> destroying local actor" one second after "sync::CreateOrAdoptPropMirror: eid=5975 bound", followed by repeating "incoming PropPose key ... eid=5975 -- no local match". Source position: the destroy gate admits zero — event_dispatch_entity.cpp:286-290 at f690c017, :236-240 at 3af5ddae, 'if (p.elementId != 0 && ...)' — so a zero-eid destroy skips the range check and falls back to key resolution; code still there at 3af5ddae. STRENGTH MEASURED on both peers in one arm (R3Q-CONTROL-01, 2026-09-04), raised by the operator before any log was read. FALSIFIER: a client OnDestroy resolving on something other than the placed mirror; the placed mirror surviving with another cause of invisibility; the host's eid=0 destroy naming a different key; or the client re-creating the actor later in the arm. FIX EXISTS: no. VERDICT: READY for design, NEEDS_RUNTIME for confirmation of scope — the source path was never read for this observation (its own GAP says so), and whether the collateral destroy depends on ordering, class, or the hand actor sharing the logical key is not established.
    evidence: C:\AgenticStaging\VOTV\Coordination\R3Q\PACKET\r3q-runtime-packet-1\evidence\FA5BDF69216FF81A-OBS-R3Q-C01-client-loses-host-placed-box.md; git show 3af5ddae:src/votv-coop/src/coop/dispatch/event_dispatch_entity.cpp:236-240
- [MEASURED] OTHER UNFIXED #3 — OBS-R3Q-P01, the host holds the lid while the client renders the box closed. Signature: host "hand_item: local hand -> cls='prop_boxCap_C' name='drivebox_lid_0' (announced, rel measured)" against client "hand_item: slot 0 mirror SPAWNED cls='prop_boxCap_C' ... (display-only, view-anchored)", with paired sampler captures showing host variant drivebox_sb (open) and a client box still offering 'Open'. STRENGTH MEASURED on both peers in paired captures (R3Q-PRIMARY-01 P0). FALSIFIER: a client frame showing the open variant or a non-closed interaction set while the host holds the lid; or a client log line applying an open-state change to the keyed box. FIX EXISTS: no. VERDICT: NEEDS_RUNTIME — no source path has been read for it, the reverse direction (client opens, host renders) was never exercised, and persistence across rejoin/resync/reload is UNKNOWN. It is the same defect family as published MVF-0002 and adds the display-only/view-anchored distinction to it.
    evidence: C:\AgenticStaging\VOTV\Coordination\R3Q\PACKET\r3q-runtime-packet-1\evidence\6295E6C4E27D0EC6-OBS-R3Q-P01-lid-state-divergence.md
- [MEASURED] OTHER UNFIXED #4 — the four published findings on remotes/experimental/findings/verified (546419cd), none of which has a fix, all against AFFECTED_RELEASE v0.9.0n-b150-dev (ba6d8c39, DLL cfd1a037...) and AFFECTED_MAIN_HEAD f690c017. MVF-0001 drive-box contents not replicated: STATUS=VERIFIED, CAUSE_STRENGTH=INFERRED, affected paths protocol.h:3500-3561 and container_contents_sync.cpp:868-886, cause assumes no unexpectedly named native integration; NEEDS_RUNTIME/DESIGN (the exact native Drive Box content field is UNKNOWN). MVF-0002 drive-box lid-state divergence: VERIFIED/INFERRED, protocol.h:3500-3561, native detach route UNKNOWN; NEEDS_RUNTIME. MVF-0003 food eat-progress stale on remote peer: STATUS=REPORTED, CAUSE_STRENGTH=HYPOTHESIS — the value sequence is reported, not measured, and no food-progress image exists; NEEDS_RUNTIME. MVF-0004 BUG-017: STATUS=PARTIALLY_VERIFIED, INFERRED, carries an explicit PROMOTION_CONDITION requiring a five-action run with every state-mutating tool INACTIVE plus complete activity and artifact manifests; NEEDS_RUNTIME and expressly NOT promoted because the run that might have promoted it was itself rejected.
    evidence: git show 546419cd:findings/MVF-0001-drive-box-contents-not-replicated.md; :findings/MVF-0002-...; :findings/MVF-0003-...; :findings/MVF-0004-...; git show 546419cd:PUBLICATION-STATUS.md
- [MEASURED] SELECTION RATIONALE for 1, 2 and 10, stated by the artifacts rather than inferred: the binding analysis review authorised implementation for exactly the four SOURCE_PROVEN candidates (1, 2, 8, 10) and refused it for the six SOURCE_CONSISTENT ones (3, 4, 5, 6, 7, 9) on the ground that the source proves the drop but not the defect causing it. Of the four authorised, 8 was withheld from the author lane by the work order for programme sequencing (its BUG-017 correlation and a NOT_CLEAN runtime packet), leaving 1, 2 and 10. The fix review then issued per-candidate publication authorisation (three independent YES rows), protocol_changed=false verified by protocol.h blob identity 0c69d319... at the pin and all three commits, and confirmed the new standalone tests are not production byte contributors in the reviewed build graph.
    evidence: ws-player-logs-review-codex\MATRIX.md Aggregate section; ws-player-log-fix-codex\WORK_ORDER.md 'The authorisation'; ws-player-log-fix-review-codex\REVIEW.md 'Per-candidate authorisation' table

# RECOMMENDATIONS
- (small) Implement candidate 8 (client placement-queue admission ordering) as a fourth independently revertible commit in the same shape as 46b77b92: a header-only policy under include/coop/props/ or include/coop/dispatch/, a single hunk in prop_drop_intent.cpp:141-149, and src/votv-coop/tests/standalone/candidate_08_place_queue_admission.cpp with a positive control, the 32-ineligible-then-one-eligible killing case, a bound assertion and an omission case.
    why: It is the only remaining candidate that is SOURCE_PROVEN, already IMPLEMENTATION_AUTHORISED by the binding review, unchanged at 3af5ddae (identical line numbers), bounded to one function, and testable with no game process. Its exclusion from the R3Q lane was programme sequencing, not an evidence gap.
    depends_on: Nothing runtime. Keep the review's constraint: do not treat the 179 observed cap hits as proven eligible losses, and do not claim it fixes BUG-017.
- (small) Rebase or cherry-pick 10777571, 46b77b92 and 15589b7f onto 3af5ddae on a fresh branch, re-run the three standalone tests and re-compile the three translation units with MSVC 14.50, then re-verify the two new headers and the protocol.h blob identity on the new base.
    why: All three fixes sit on the orphaned f690c017 and none of their content reached new upstream main, so they are currently unshippable. Every target region is logic-identical at 3af5ddae, so the ports are line-shift-only and the existing tests remain valid controls.
    depends_on: A scratch worktree at 3af5ddae (C1\WORKTREES\scratch-upstream-3af5ddae already exists).
- (medium) Open a source-reading lane for OBS-R3Q-C01: read the destroy path (event_dispatch_entity.cpp:236-240 and remote_prop_destroy.cpp key-fallback resolution at 3af5ddae) and determine whether a zero-eid keyed DESTROY should be refused, scoped to the exact actor it names, or ordered against a just-bound mirror.
    why: It is the only MEASURED-on-both-peers defect with no fix and no source analysis at all — its own GAP says no source path has been read. It is the destroy-side twin of candidate 2, which is already fixed, so the spawn-side reasoning and its gate helper are directly reusable.
    depends_on: Read-only source work; a rig confirmation of scope (ordering vs class vs shared hand-actor key) can follow, not precede, the source read.
- (small) Do not open fix work on candidates 3, 4, 5, 6, 7 or 9. Instead, carry their existing PROBE sections forward verbatim as the rig plan for the next authorised runtime session, and re-count candidate 6's signature on 3af5ddae before reusing its 214 figure.
    why: Each has IMPLEMENTATION_AUTHORISED NO from a binding review that read the source itself; candidate 4 is additionally 29/31 explained by ragdoll attachment. Candidate 6's log signature was split into a sustained-stream WARN and an ordinary INFO by upstream's unresolved_pose_ledger, so any count carried over from the b150 logs would be measuring a signature that no longer exists in that form.
    depends_on: Rig ownership and an authorised runtime arm.
- (medium) Decide publication for 1c393401 (baseline delivery-invariant) separately from the player-log fixes, and treat its port as a real merge rather than a line shift.
    why: It is reviewed BASELINE_FIX_CLEAN and publication-authorised but unpublished, and its file interactable_channel.h is the single most-changed file in the upstream rewrite (463 changed lines, 119 non-comment), so unlike the other four it will not port mechanically. Its latency gap — whether SendReliableToSlot can fail at all — must travel with it wherever it is quoted.
    depends_on: A decision on whether anything is published at all this session; the C1 brief forbids pushing.

# GAPS
- Whether any of these sites is identical on the SHIPPED b150 build (ba6d8c39) was not checked. The player logs came from b150, the candidates were traced at f690c017, and b150 is not an ancestor of new main; every source position in this inventory is verified at f690c017 and 3af5ddae only.
- No runtime observation was made and no game process was started. Every player-visible consequence for all ten candidates remains UNKNOWN, and that is the reviewers' own position, not a limitation introduced here.
- Candidate 6's mechanism was confirmed unchanged at 3af5ddae by reading the function, but the new unresolved_pose_ledger's thresholds (kSustainedCount / kSustainedMs) were not read, so how many of the original 214 drops would now raise a WARN is unquantified.
- Candidate 2's four sender sites were re-verified only for existence of the receive-side gate and the OnSpawn keyed-zero branch at 3af5ddae; the four kInvalidId-to-0u assignments in prop_lifecycle.cpp, prop_snapshot.cpp, prop_container_extract.cpp and trash_collect_sync.cpp were confirmed by the binding review at f690c017 and were NOT re-read at 3af5ddae.
- The candidate 8 fix sketch is a design proposal read off the source, not an implemented or compiled change; the eviction policy (evict oldest ineligible) is my selection among several admissible shapes and has not been reviewed.
- Whether OBS-R3Q-C01's collateral destroy is reachable at 3af5ddae was inferred from the destroy gate still admitting elementId==0; remote_prop_destroy.cpp's key-fallback resolution was not re-read at the new head.
- The C1 CONTEXT.md figures for upstream drift (file counts, non-comment line counts) were taken as given from the brief and not independently recomputed here.
