# SUMMARY
All four fix commits target defects that are STILL PRESENT at both new targets, so none of them is a fix for an absent defect. On private/b150-aligned 998c43b6 all four cherry-pick CLEANLY, individually and as an in-order chain, and the applied source hunks are byte-for-byte identical to the originals (49/35/41/185 changed source lines each way). On new upstream main 3af5ddae, 10777571 and 15589b7f apply CLEANLY; 46b77b92 conflicts in one file with one hunk and 1c393401 conflicts in three files with seven hunks — every one of those eight conflicts is upstream's comment-rewrite sweep colliding with the fix's added code, not a competing semantic change, and all resolve mechanically by keeping upstream's comment and taking the fix's code. The resolved upstream chain applies with zero residual markers and the same code as the b150 chain. session.cpp auto-merged on both targets with no conflict. The commit message claim in 1c393401 is verified at the new upstream: the steady-poll SendReliable is still a fan-out returning anySuccess (3af5ddae session.cpp:197/204/206), so the naive "bank the opposite value" alternative is still insufficient there. The standalone candidate_01/02/10 tests still compile against both targets' headers (log::Write signature, ParamInfo shape, WireKey/PropSpawnPayload, and the Registry eid predicates are unchanged at both). The tests/baseline_delivery harness is the one real retarget cost: it compiles the REAL interactable_channel.h against a stub tree, and 3af5ddae's header now uses R::OuterOf and R::ClassNameOf and pulls in four new real headers the stub set does not cover, so the harness will not compile there without stub extension; on 998c43b6 the header has zero non-comment drift from the fixes' base and the harness stubs are still sufficient. Separately, run.ps1 hard-pins the negative control to the now-orphaned f690c017 and must be repointed at whichever target is adopted.

# FACTS
- [MEASURED] Scratch worktrees verified clean at their named HEADs before work and restored to them after: scratch-upstream-3af5ddae HEAD=3af5ddaed3957e0ee3bb9708e453a4642eabab6f, status empty; scratch-b150-998c43b6 HEAD=998c43b6e7db43c844ef702cff98ed2dc4eb1a2f, status empty. Nothing was pushed and no commit was left behind.
    evidence: git -C C:/AgenticStaging/VOTV/Coordination/C1/WORKTREES/scratch-upstream-3af5ddae rev-parse HEAD -> 3af5ddaed3957e0ee3bb9708e453a4642eabab6f; git status --short -> (empty). Same two commands in scratch-b150-998c43b6 -> 998c43b6e7db43c844ef702cff98ed2dc4eb1a2f / (empty).
- [SOURCE_PROVEN] 10777571 defect (a failed ParamFrame named write leaves the frame dispatchable, so untouched/default bytes reach ProcessEvent) is PRESENT at 3af5ddae. Mechanism of the fix: a writeFailed_ poison bit set on every SetRaw failure path, a stricter ready() boundary, and Call() gated on ready() instead of valid(), plus first-failure-only diagnostics keyed by (UFunction, reason, param).
    evidence: 3af5ddae:src/votv-coop/include/ue_wrap/core/call.h:27 declares only `bool valid() const { return fn_ != nullptr; }` (no ready(), no writeFailed_). 3af5ddae:src/votv-coop/src/ue_wrap/core/call.cpp:114 `if (fn_ == nullptr || buf_.empty()) return false;`, :117 `UE_LOGE("ParamFrame::Set: unknown param '%ls'", name); return false;`, :146 `if (!frame.valid()) return false;`
- [SOURCE_PROVEN] 10777571 defect is PRESENT at 998c43b6 in the same shape.
    evidence: 998c43b6:src/votv-coop/include/ue_wrap/core/call.h:27 `bool valid() const { return fn_ != nullptr; }`; 998c43b6:src/votv-coop/src/ue_wrap/core/call.cpp:120 `if (fn_ == nullptr || buf_.empty()) return false;`, :123 unknown-param return, :152 `if (!frame.valid()) return false;`
- [SOURCE_PROVEN] 46b77b92 defect (a PropSpawn carrying the protocol's no-EID sentinel 0 plus a usable logical Key is dropped at the inbound boundary, because both range predicates reject 0) is PRESENT at 3af5ddae. The fix hoists the policy into coop/dispatch/prop_spawn_gate.h and admits elementId==0 only when the Key carries identity (non-empty, len<=31, not "None"), leaving every nonzero range rule intact.
    evidence: 3af5ddae:src/votv-coop/src/coop/dispatch/event_dispatch_entity.cpp:181-183 is the unhoisted inline range test on p.elementId with no zero-sentinel branch; 3af5ddae:src/votv-coop/include/coop/element/registry.h:92 `return id != kInvalidId && id >= 1u && id < kHostRangeSize;` and :95 `id >= kHostRangeSize` — both false for id==0. include/coop/dispatch/ contains only event_feed.h (no prop_spawn_gate.h).
- [SOURCE_PROVEN] 46b77b92 defect is PRESENT at 998c43b6 in the same shape.
    evidence: 998c43b6:src/votv-coop/src/coop/dispatch/event_dispatch_entity.cpp:212-214 inline range test; 998c43b6:src/votv-coop/include/coop/element/registry.h:166 `id >= 1u` and :169 `id >= kHostRangeSize`; include/coop/dispatch/ contains only event_feed.h.
- [SOURCE_PROVEN] 15589b7f defect (RetryPendingTick tests the deadline BEFORE sampling resolution, so an entry whose actor resolved on the same tick it expires is logged as 'expired (actor never resolved)' and discarded instead of applied) is PRESENT at 3af5ddae. The fix samples resolvability first and routes through ClassifyPendingRetry, which returns Apply for a resolvable entry even at/after the deadline.
    evidence: 3af5ddae:src/votv-coop/src/coop/interactables/drive_sync.cpp:418 `if (now >= pd.until) {` immediately followed by :419 `UE_LOGW("drive_sync: pending apply kind=%d expired (actor never resolved)", pd.kind);` and `continue;`, all before any LivePropActor call. include/coop/interactables/pending_retry.h absent from the 3af5ddae tree.
- [SOURCE_PROVEN] 15589b7f defect is PRESENT at 998c43b6 in the same shape.
    evidence: 998c43b6:src/votv-coop/src/coop/interactables/drive_sync.cpp:421-422 (same deadline-first branch and same log string); pending_retry.h absent from the 998c43b6 tree.
- [SOURCE_PROVEN] 1c393401 defect (the connect snapshot discards SendReliableToSlot's result, banks lastKnown_ and increments the counter regardless, so a refused join-time send leaves the host believing the peer holds state it never received and the steady poll sees no delta forever) is PRESENT at 3af5ddae.
    evidence: 3af5ddae:src/votv-coop/include/coop/interactables/interactable_channel.h:359 `s->SendReliableToSlot(peerSlot, a_.kind, &p, sizeof(p));` with the return value discarded, :360 `{ std::lock_guard<std::mutex> lk(stateMutex_); lastKnown_[d.first] = on; }`, :361 `++sent;`, :363 the log line reporting `sent` as deliveries.
- [SOURCE_PROVEN] 1c393401 defect is PRESENT at 998c43b6 in the same shape.
    evidence: 998c43b6:src/votv-coop/include/coop/interactables/interactable_channel.h:426 discarded SendReliableToSlot, :427 unconditional lastKnown_ bank, :428 `++sent;`, :430 the log line.
- [SOURCE_PROVEN] The prerequisite the 1c393401 fix depends on exists at BOTH targets: Session already exposes peerGenerationForSlot backed by peerGenBySlot_, so the generation stamp needs no new plumbing.
    evidence: 3af5ddae:src/votv-coop/include/coop/net/session.h:325-327 peerGenerationForSlot, :554 `std::array<std::atomic<uint32_t>, kMaxPeers> peerGenBySlot_{};`. 998c43b6:src/votv-coop/include/coop/net/session.h:507-509 and :868 the same two.
- [SOURCE_PROVEN] VERIFIED at 3af5ddae, as the 1c393401 commit message asserts: the steady-poll SendReliable is still a fan-out returning anySuccess across peers, so banking the global baseline on another peer's success would still leave the original target never retried — the naive one-line alternative remains insufficient at the new upstream.
    evidence: 3af5ddae:src/votv-coop/src/coop/net/session.cpp:197 `bool anySuccess = false;`, :198-205 the `for (int i = 0; i < kMaxPeers; ++i)` loop with :204 `if (backlog_.SendOrQueue(i, laneIdx, hConn, wire, total)) anySuccess = true;`, :206 `return anySuccess;`. The channel's poll path calls s->SendReliable (grep of R:: / s-> surface in 3af5ddae interactable_channel.h shows s->SendReliable present).
- [MEASURED] On 998c43b6 all four commits cherry-pick CLEANLY, individually (each measured from a fresh reset) and as an in-order chain. session.cpp auto-merged with no conflict. Chain head 316da1bf, 31 files changed, 1482 insertions, 33 deletions, zero conflict markers.
    evidence: git cherry-pick -x <sha> in scratch-b150-998c43b6: exit=0 for 10777571 (3 files, +124/-5), 46b77b92 (3 files, +101/-6), 15589b7f (3 files, +76/-12), 1c393401 (22 files, +1181/-10, 'Auto-merging src/votv-coop/src/coop/net/session.cpp' with no CONFLICT). Chain run: all four report 'clean'; git log --oneline -5 -> 316da1bf/a2579ed4/4ac657dc/64050cb1/998c43b6.
- [MEASURED] The b150 applications are byte-equivalent to the original commits in the production source: applied vs original changed-source-line counts match exactly for all four.
    evidence: For each sha, `git diff HEAD~1 HEAD -- src/votv-coop/include src/votv-coop/src | grep -c '^[+-][^+-]'` vs `git diff <sha>~1 <sha> -- ...` -> 10777571: applied=49 original=49; 46b77b92: 35/35; 15589b7f: 41/41; 1c393401: 185/185.
- [MEASURED] On 3af5ddae, 10777571 and 15589b7f cherry-pick CLEANLY (exit=0, auto-merging call.h/call.cpp and drive_sync.cpp respectively).
    evidence: git cherry-pick -x in scratch-upstream-3af5ddae: 10777571 exit=0, 'Auto-merging src/votv-coop/include/ue_wrap/core/call.h', 'Auto-merging src/votv-coop/src/ue_wrap/core/call.cpp', 3 files +124/-5; 15589b7f exit=0, 'Auto-merging .../drive_sync.cpp', 3 files +76/-12.
- [MEASURED] On 3af5ddae, 46b77b92 conflicts in exactly one file with exactly one hunk, and the conflict is COMMENT-ONLY DRIFT: upstream's sweep replaced the 26-line PR-FOUNDATION-1/Fork-B-2a comment block with a 6-line restatement. The fix's code (the IsAllowedInboundPropSpawn call and the key-carrying log line) applied cleanly below the marker; prop_spawn_gate.h and candidate_02 added with no conflict. Minimal resolution: keep upstream's comment, append the two-line zero-sentinel sentence, take the fix's code unchanged.
    evidence: git cherry-pick -x 46b77b92 -> 'CONFLICT (content): Merge conflict in src/votv-coop/src/coop/dispatch/event_dispatch_entity.cpp'; status 'A prop_spawn_gate.h / UU event_dispatch_entity.cpp / A candidate_02'. Conflict region lines 173-207: HEAD side is 6 comment lines, theirs side 26 comment lines, no code on either side; lines 208-218 (the const bool ok = coop::dispatch::IsAllowedInboundPropSpawn(...) and the key='%.*s' log) are outside the markers. Resolved diff in C:\AgenticStaging\VOTV\Coordination\C1\EVIDENCE\retarget-46b77b92-upstream3af5ddae.patch
- [MEASURED] On 3af5ddae, 1c393401 conflicts in three files with seven hunks (interactable_channel.h 4, session.h 2, interactable_sync.cpp 1); session.cpp AUTO-MERGED with no conflict. Every conflict is the fix's added code landing adjacent to a comment upstream rewrote — there is no competing semantic change on the HEAD side in any of the seven. Minimal resolution: keep upstream's comment text, take the fix's code lines.
    evidence: git cherry-pick -x 1c393401 -> CONFLICT in interactable_channel.h, session.h, interactable_sync.cpp; 'Auto-merging src/votv-coop/src/coop/net/session.cpp' with status 'M' (not UU). Per-hunk: channel #1 HEAD=2 comment lines vs theirs=3 code lines+3 comment; #2 HEAD=3 comment+`int sent = 0;` vs theirs=7 comment+3 counters; #3 HEAD=2 comment vs theirs=`RetryPendingDeliveries(s);`+2 comment; #4 HEAD=2 member decls vs theirs=pendingDeliveries_ decl + the same 2 reworded; session.h #1 HEAD=TrySendReliableToSlot comment vs theirs=SendReliableToSlotForGeneration decl + that comment; #2 HEAD=KickClaimed comment vs theirs=SendReliableToSlotImpl_ decl + that comment; interactable_sync.cpp HEAD=1 door line+trailing comment vs theirs=the 7 per-channel OnPeerLeft calls. Resolved diff in C:\AgenticStaging\VOTV\Coordination\C1\EVIDENCE\retarget-1c393401-upstream3af5ddae.patch (143/14/12/26 source lines, vs the original's 143/14/13/26 — the one-line delta is upstream's shorter door comment).
- [MEASURED] The 3af5ddae resolution produces a coherent session.cpp: SendReliableToSlot delegates to SendReliableToSlotImpl_(peerSlot, 0, ...), the new SendReliableToSlotForGeneration refuses generation 0 and the save-transfer kinds, and Impl_ carries the two generation reads bracketing the peerConns_ handle capture, all against upstream's rewritten comments.
    evidence: Post-resolution scratch-upstream-3af5ddae src/votv-coop/src/coop/net/session.cpp:79-120 read back showing the three functions and both `if (expectedGeneration != 0 && peerGenBySlot_[peerSlot].load(std::memory_order_acquire) != expectedGeneration) return false;` guards, one before and one after `const uint32_t hConn = peerConns_[peerSlot].load();`
- [MEASURED] The full four-commit chain applies on 3af5ddae with the SAME conflict set as the independent picks (the four commits touch disjoint file sets), and after the same mechanical resolutions leaves zero conflict markers. Chain head 78645cbd, 31 files changed, 1481 insertions, 31 deletions.
    evidence: Chain run in scratch-upstream-3af5ddae: 10777571 clean; 46b77b92 CONFLICT in event_dispatch_entity.cpp (1 hunk resolved); 15589b7f clean; 1c393401 CONFLICT in interactable_channel.h/session.h/interactable_sync.cpp (4+2+1 hunks resolved); git log --oneline -5 -> 78645cbd/26ed33c6/91d172f3/f794933a/3af5ddae; `grep -rn -I '^<<<<<<< ' src/votv-coop/` empty. Patch: C:\AgenticStaging\VOTV\Coordination\C1\EVIDENCE\retarget-chain-all4-upstream3af5ddae.patch
- [SOURCE_PROVEN] The standalone candidate_10 test is self-contained (an __has_include fallback that carries the pre-fix policy inline) and compiles against either target unchanged.
    evidence: src/votv-coop/tests/standalone/candidate_10_pending_retry_order.cpp:1-13 — `#if __has_include("coop/interactables/pending_retry.h")` with an inline deadline-first fallback definition; the rest of the file includes only <cstdio>.
- [SOURCE_PROVEN] The standalone candidate_02 test's real-header dependencies are unchanged at both targets, so it still compiles: PropSpawnPayload keeps elementId and a WireKey key{uint8_t len; char data[31];} (static_assert sizeof==32), and Registry::IsAllowedHostAllocatedEid/IsAllowedPeerAllocatedEid keep their signatures.
    evidence: candidate_02 includes coop/element/registry.h and coop/net/protocol.h and uses payload.elementId / payload.key.len / payload.key.data. 3af5ddae:protocol.h:758-762 and :781; 998c43b6:protocol.h:3183-3187 and PropSpawnPayload at :3620 — identical WireKey shape. registry.h predicates at 3af5ddae:91-96 and 998c43b6:165-170.
- [SOURCE_PROVEN] The standalone candidate_01 test's real-header dependencies are unchanged at both targets: ue_wrap::log::Write(Level, const char*, ...) and reflection's ParamInfo{std::wstring name; int32_t offset; int32_t size; uint64_t flags;} with FunctionParams(void*)/FunctionFrameSize(void*)/CallFunction(void*,void*,void*).
    evidence: 3af5ddae:include/ue_wrap/core/log.h:22 and 998c43b6:include/ue_wrap/core/log.h:19 both `void Write(Level level, const char* fmt, ...);`. ParamInfo body read at 3af5ddae:reflection.h:199 and 998c43b6:reflection.h:246 — field-for-field identical; FunctionParams/FunctionFrameSize/CallFunction declared at both.
- [SOURCE_PROVEN] The tests/baseline_delivery harness compiles the REAL include/coop/interactables/interactable_channel.h and the REAL src/coop/net/send_backlog.cpp against a stub tree placed FIRST on the include path, so anything the target's channel header newly requires must be stubbed.
    evidence: src/votv-coop/tests/baseline_delivery/run.ps1:10-13 ($IncludeRoot/$StubRoot/$Harness/$BacklogSource) and :44-47 `cl /nologo /std:c++20 /EHsc /W4 /WX /I"$StubRoot" /I"$HeaderRoot" /I"$IncludeRoot" ... "$Harness" "$BacklogSource"`; qualification.cpp:3 `#include "coop/interactables/interactable_channel.h"`.
- [SOURCE_PROVEN] On 998c43b6 the baseline_delivery harness still compiles as shipped: interactable_channel.h has ZERO non-comment drift from the fixes' base f690c017, an identical include list, and the identical reflection surface {NameOf, ToString, IsLive, IsLiveByIndex, InternalIndexOf} that the stub provides; send_backlog.h and send_backlog.cpp are byte-identical to f690c017.
    evidence: `git diff -w f690c017 998c43b6 -- .../interactable_channel.h` non-comment +/- line count = 0. Include lists at f690c017:21-41 and 998c43b6:21-41 are line-for-line identical. `git diff --stat f690c017 998c43b6 -- send_backlog.cpp send_backlog.h` -> empty. R:: surface per base: f690c017 and 998c43b6 both {InternalIndexOf, IsLive, IsLiveByIndex, NameOf, ToString}; stub ue_wrap/core/reflection.h declares exactly those five.
- [SOURCE_PROVEN] On 3af5ddae the baseline_delivery harness will NOT compile as shipped. The target's channel header uses R::OuterOf and R::ClassNameOf, which the stub ue_wrap/core/reflection.h — which SHADOWS the real one, being first on the include path — does not declare; these uses sit in a non-template member of the non-template class Channel, so they are parsed unconditionally. The header also newly includes four real headers the stub set does not cover, one of which (ue_wrap/actors/prop.h) declares `reflection::FName GetInteractableKey(void* obj);` against that same stub.
    evidence: 3af5ddae:interactable_channel.h:38 `namespace R = ue_wrap::reflection;`, :88 `class Channel {` (not a template), :533 `void* const outer = R::OuterOf(obj);`, :546 and :561 `R::ClassNameOf(...)`. Stub file scratch tree src/votv-coop/tests/baseline_delivery/stubs/ue_wrap/core/reflection.h declares only NameOf/ToString/IsLive/InternalIndexOf/IsLiveByIndex. New includes at 3af5ddae:16 coop/element/portable_identity.h, :18 ue_wrap/engine/engine.h (which itself pulls 11 further real engine headers), :23 ue_wrap/actors/prop.h, :24 ue_wrap/core/sdk_profile.h; 3af5ddae:ue_wrap/actors/prop.h:13-14 and :66 `reflection::FName GetInteractableKey(void* obj);`. Other new symbol needs in the same block: ue_wrap::profile::off::UObject_ObjectFlags (:535, :548), ue_wrap::engine::TryGetActorLocation (:537), ue_wrap::engine::ParentActorOf (:541), ue_wrap::prop::GetInteractableKeyString/GetActorSaveKeyString (:550, :552), coop::element::PortableIdentity (:483).
- [SOURCE_PROVEN] run.ps1 hard-pins its negative control to the orphaned commit f690c017, not to the retarget target. On either target the control would assert against a tree that is no longer the target's pre-fix image; on 3af5ddae that commit is not even an ancestor of the new upstream main.
    evidence: src/votv-coop/tests/baseline_delivery/run.ps1:14 `$Pinned = 'f690c017e8e55f2996d0e9121956aae66486742e'`, :15 `$HeaderRel = 'src/votv-coop/include/coop/interactables/interactable_channel.h'`, :120 `Write-GitBlob "$Pinned`:$HeaderRel" $PinnedHeader`. CONTEXT.md records f690c017 as the old upstream main orphaned by the 2026-09-08 force-rewrite.
- [MEASURED] tests/ does not exist at either target before the picks, so no test-file collision is possible and all test additions are pure adds.
    evidence: `git ls-tree -d --name-only 3af5ddae -- src/votv-coop/tests/` -> empty; same for 998c43b6. After the picks, src/votv-coop/tests/standalone/ contains exactly candidate_01/02/10.
- [MEASURED] None of the three standalone candidate tests has an in-tree build or run script; only the three .cpp files were added.
    evidence: `git grep -ln 'candidate_01_param_frame_boundary|candidate_02_prop_spawn_gate|candidate_10_pending_retry_order' 15589b7f` -> no hits. `git ls-tree -r --name-only 15589b7f -- src/votv-coop/tests/` lists only the three .cpp files.

# RECOMMENDATIONS
- (small) Retarget all four fixes onto private/b150-aligned 998c43b6 first. Cherry-pick in order 10777571 -> 46b77b92 -> 15589b7f -> 1c393401; nothing needs resolution and the applied source is byte-identical to the reviewed commits, so the existing per-candidate reviews carry over without re-review of the bytes.
    why: Measured clean on all four individually and as a chain, with matching changed-line counts. This is the private playable line, so it is also the line where these four fixes can be exercised on the rig.
    depends_on: None
- (medium) Retarget onto upstream 3af5ddae as a second, separate pass using the recorded resolutions: apply the two clean picks unchanged, then resolve 46b77b92's single comment-hunk and 1c393401's seven comment-adjacent hunks by keeping upstream's comment prose and taking the fix's code verbatim. The exact resolved trees are in the EVIDENCE patches and the resolution scripts are re-runnable.
    why: All eight conflicts are upstream's comment-rewrite sweep, not competing logic; the resolution is mechanical and was measured to produce the same code as the b150 chain. Doing it as a separate pass keeps the b150 line unblocked by the upstream test-stub work.
    depends_on: None
- (medium) Before claiming the 1c393401 qualification still holds on 3af5ddae, extend tests/baseline_delivery/stubs/ue_wrap/core/reflection.h with OuterOf and ClassNameOf, and add stubs (or shadowing headers) for coop/element/portable_identity.h, ue_wrap/engine/engine.h, ue_wrap/actors/prop.h and ue_wrap/core/sdk_profile.h so the real channel header parses. Do not report the harness as passing on that target until it compiles.
    why: The harness compiles the real channel header against a stub tree that is first on the include path. 3af5ddae's header uses two reflection entry points the stub does not declare, in a non-template member that is always parsed, plus four new real includes. Reporting the fix as qualified on the new upstream without this is exactly the absent-probe defect class.
    depends_on: the 3af5ddae retarget pass
- (small) Repoint run.ps1's negative control from the hard-coded $Pinned = f690c017 to the actual retarget target sha (998c43b6 or 3af5ddae), so the pre-fix control is built from the target's own pre-image rather than an orphaned commit.
    why: A control must assert the property against the tree the candidate is being applied to. f690c017 is orphaned by the upstream force-rewrite and is not an ancestor of the new main, so on that target the control measures a tree the candidate never touched. On b150 the header content happens to match, which makes the wrong provenance silent rather than absent.
    depends_on: None
- (small) Treat the three standalone candidate tests as a build gap: add a minimal runner (a run.ps1 in tests/standalone or CMake targets) that compiles candidate_01 with src/ue_wrap/core/call.cpp and candidate_02/10 header-only, at whichever target is adopted.
    why: Nothing in the tree builds or runs them, so their status at a new target is a source-shape argument only. candidate_02 and candidate_10 carry __has_include pre-fix fallbacks that are meant to prove the negative before the fix header exists, and that design is only worth anything if something actually runs both arms.
    depends_on: None
- (small) Do not drop any of the four as already-fixed-upstream. Re-confirm the four defect citations at whatever sha is finally adopted before applying, since upstream main is being force-rewritten.
    why: All four defects were measured present at both targets today, but the upstream line was rewritten once already in this window and the cited line numbers are sha-specific.
    depends_on: None

# GAPS
- Nothing was built and nothing was run, per the task constraint. Every compile claim about the standalone tests and the baseline_delivery harness is a source-shape argument from reading the stubs against the real headers, not a compiler result. The 3af5ddae harness break is strongly evidenced (R::OuterOf / R::ClassNameOf are used unconditionally and are absent from the shadowing stub) but has not been confirmed by cl.exe, and there may be ADDITIONAL breaks beyond the ones named once those are fixed.
- Runtime behaviour of the retargeted fixes at either target is UNKNOWN. Defect presence was established from source; that the fixes still repair the defect at the new targets follows from the code being byte-identical on b150, but was not exercised on the rig or against the oracle.
- Whether the 1c393401 qualification's 10 tests and 10 omission cases still PASS at either target is UNKNOWN — only their compilability was reasoned about, and only the harness's ability to build was assessed.
- The 3af5ddae conflict resolutions preserve the fix's code exactly and keep upstream's comment prose, but whether upstream's rewritten comments now MISDESCRIBE the merged behaviour was not audited line by line — in particular the PropSpawn boundary comment at event_dispatch_entity.cpp and the connect-snapshot comment in interactable_channel.h, where the shortened upstream text no longer mentions the accounting the fix changes.
- Only the four named fix commits were measured. Whether other content on fix/player-log-candidates-20260904 or fix/baseline-delivery-invariant-20260904 (or their branch tips beyond these commits) also needs retargeting was not examined.
- 3af5ddae's interactable_channel.h has 119 non-comment changed lines vs f690c017 overall; only the subset that collides with the fixes and the subset that affects the test stubs was examined. The remaining upstream changes in that file (the identity probe, portable-identity indexing) were not reviewed for interaction with the pending-delivery records the fix adds — notably whether keys can now be portable-identity names rather than game keys, which would change what a queued PendingDelivery key resolves to on retry.
- f690c017 is currently reachable in the local S:\GAMEMODDING\Multivoid-Private object store, which is why run.ps1's pinned blob read would still succeed today; whether it survives a gc or a fresh clone was not tested.
