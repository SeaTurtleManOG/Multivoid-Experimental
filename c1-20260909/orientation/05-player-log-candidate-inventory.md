# SUMMARY
The design draft is written to C:\AgenticStaging\VOTV\Coordination\C1\LANES\bug017-design\BUG017-CONTRACT-DESIGN-DRAFT.md (68,193 bytes, 994 lines, sha256 c1a54cbba8c93c0badf8c49ad1ea9e6a8ffff357477b19b75f041d34b2d43bc4), bounded to ADD_CONTRACT_LAYER_KEEP_CURRENT_TRANSPORT. The main repo was not modified: still on private/dev with only the pre-existing untracked .r3q-bootstrap/. The central finding is that the BUG-017 mechanism is CODE-IDENTICAL across ba6d8c39 (b150), f690c017 and 3af5ddae — b150 and f690c017 are byte-identical in every mechanism file, and b150 to 3af5ddae changes only comments plus one warn-once latch and one dead function; PropDropIntentPayload's body is byte-for-byte identical. The defect is a contract violation between two identity domains: container contents are host-arbitrated state addressed by a transient element id (ContainerContents=118, "eid-addressed"), while custody transfers across an actor gap addressed by a persistent logical key (PropDropIntent=90, a fixed 172-byte single-datagram POD with no contents field and no room for one). HostSpawnPlacedProp rebuilds the container from class+key+transform+parity alone — no loadData, no index install, no contents read — so the host's populated GObjStack slot is left orphaned (INFERRED) and the fresh propInventory init allocates an empty one. The second finding is that BUG-017 is closable on the HOST ALONE with zero wire bytes: a keyed custody park captured in DestroyResolvedLocalActor_ (remote_prop_destroy.cpp:73-99, before K2_DestroyActor at :98) and consumed in HostSpawnPlacedProp (prop_drop_intent.cpp:158-232, after FinishDeferredSpawn at :214), refused whenever the fresh slot is non-empty so the native host-authored path still wins. That makes the private b150 line (which must not change the wire) and upstream (where a bump is cheap because mismatched peers are refused at session.cpp:262-267) take the SAME patch text. The plan touches two production files by three lines each plus one new header-only module, with the decidable logic behind an engine-free predicate so it is testable in the tests/baseline_delivery and candidate_XX styles. OBS-R3Q-C01 does not block the fix but does block any client-side acceptance arm, so arms A1-A3 and A5 are scored host-only and A4 is marked BLOCKED_ON_OBS_R3Q_C01. Seven open questions carry their settling observable; Q-8 closed by measurement during the work.

# FACTS
- [MEASURED] The BUG-017 mechanism is code-identical between ba6d8c39 (b150 tag), f690c017 (old main) and 3af5ddae (new main). b150 and f690c017 are byte-identical in every mechanism file (container_contents_sync.{cpp,h}, prop_drop_intent.{cpp,h}, save_record_wire.{cpp,h}, event_dispatch_intent.cpp, remote_prop_destroy.cpp, prop_destroy_seam.cpp).
    evidence: git diff -w --numstat ba6d8c39 f690c017 -- <each file> returns 0/0 for all nine files. b150->3af5ddae non-comment changed lines: container_contents_sync.cpp 7, .h 0, prop_drop_intent.cpp 14, .h 0, save_record_wire 0, event_dispatch_intent.cpp 14, remote_prop_destroy.cpp 2, prop_destroy_seam.cpp 31.
- [MEASURED] The only executable b150->3af5ddae differences in the mechanism files are a warn-once latch on the contents-lane verb-registration failure log and the removal of a dead SyncDestroyedTrackedProp function. Everything else is include-comment and case-comment text.
    evidence: container_contents_sync.cpp:694-706 (the s_saidFailed latch); prop_destroy_seam.cpp diff removes SyncDestroyedTrackedProp and its mirror_manager.h include; prop_drop_intent.cpp / event_dispatch_intent.cpp / remote_prop_destroy.cpp diffs contain only comment lines.
- [MEASURED] PropDropIntentPayload is byte-for-byte identical on b150 and 3af5ddae (172 bytes; className[64], key[32], propName[32], 9 floats, physFlags+3 pad, savedScalar). Only its trailing comment and static_assert message text differ. PropDropIntent=90, ReelEjectIntent=104 and ContainerContents=118 are the same numbers on all three pins.
    evidence: diff of the struct body between the two blobs shows only comment lines; 3af5ddae:src/votv-coop/include/coop/net/protocol.h:1099-1112; kind numbers grepped identical at ba6d8c39/f690c017/3af5ddae.
- [SOURCE_PROVEN] PropDropIntentPayload has no contents field and cannot acquire one: it is a fixed 172-byte POD asserted to fit one reliable datagram, while a container may hold up to 512 variable-length SaveRecords.
    evidence: 3af5ddae:src/votv-coop/include/coop/net/protocol.h:1111 (==172) and :1112 (<= 256-20-8); container_contents_sync.cpp:55 (kMaxRecordsPerContainer = 512); save_record_wire.h:55-56; save_record.h:29-45.
- [SOURCE_PROVEN] The host's materialization path rebuilds the container from identity alone. HostSpawnPlacedProp does FindClass, BeginDeferredSpawn, setKey, WriteSpParityIdentity, FinishDeferredSpawn, SetActorScale3D and ApplySavedScalarForClass — it never calls loadData, never writes propInventory.Index, never touches GObjStack and never reads prior contents.
    evidence: 3af5ddae:src/votv-coop/src/coop/props/prop_drop_intent.cpp:158-232 (complete function body); its caller OnPropDropIntent at :377-400.
- [SOURCE_PROVEN] Container contents are eid-addressed end to end while custody is key-addressed end to end; a pickup/re-place cycle destroys the eid and mints a new one, so the only identity that survives the transition is the one the contents lane never speaks.
    evidence: protocol.h:589 ("eid-addressed"); PackContents blob [u8 op][u32 eid][u64 baseHash][u16 n] at container_contents_sync.cpp:307-316; ApplyContents resolves LivePropActor(eid) at :471-473; OnVerbEntry resolves the eid at the verb edge at :651-654; PropDropIntentPayload carries key and no eid at protocol.h:1099-1110. R3Q measured the same logical key at EID 4129->5975 (control) and 5968->4202 (primary/reload).
- [SOURCE_PROVEN] No searched host path clears the container's GObjStack slot on destroy. The string GObjStack does not occur in remote_prop_destroy.cpp, prop_destroy_seam.cpp or event_dispatch_entity.cpp on 3af5ddae.
    evidence: git show 3af5ddae:<each file> | grep -c GObjStack returns 0, 0, 0. The destroy chain is prop_destroy_seam.cpp:128-149 -> event_dispatch_entity.cpp:215-256 -> remote_prop_destroy.cpp:108-192 -> DestroyResolvedLocalActor_:73-99.
- [INFERRED] Survival of the host's old populated slot (orphaned rather than cleared) is INFERRED, not implementation evidence; whether the native prop_container/propInventory teardown clears or reclaims it is UNKNOWN.
    evidence: Fact above (absence over the searched coop paths) plus the R3Q readiness brief's own tag: 'The searched host destroy paths contain no explicit whole-slot clear/removal. Positive survival of the old slot is therefore INFERRED.'
- [SOURCE_PROVEN] The client's pocketed container record does reach the host, but only as opaque persistence bytes: the host writes a client's inventory blob to that player's per-slot file and applies nothing to its own world.
    evidence: 3af5ddae:src/votv-coop/src/coop/items/player_inventory_sync.cpp:414-435 (e.blob = std::move(blob); FlushSlot(senderPeerSlot)); the record grammar at inventory_wire.cpp:36-45.
- [SOURCE_PROVEN] Boundary 1 (refuse propInventory_C.Player != 0 AND refuse an unresolvable Player offset) is a shipped fail-closed rule applied on the read, apply, join-seed and dev-seam paths; any new entry point must apply it.
    evidence: 3af5ddae:src/votv-coop/src/coop/props/container_contents_sync.cpp:191-195 (IsWorldContainerInventory), applied at :418, :486-490, :776, :797, :809.
- [SOURCE_PROVEN] Reusing the shipped ReadContents for a HOST-LOCAL capture would silently empty every nested container, because ReadContents neuters nested indices for the wire and a host-local reattach keeps the same peer's array where those indices are valid.
    evidence: container_contents_sync.cpp:285 calls NeuterNestedIndex inside ReadContents; NeuterNestedIndex at :252-262 sets ints[0][0] = -1 explicitly because 'that index names a slot in the SENDER's array'.
- [MEASURED] ReadContents has exactly one call site and lives in an anonymous namespace, so a defaulted neuterNested parameter is safe but the custody module cannot call it directly — reuse must go through new public wrappers in container_contents_sync.h.
    evidence: git grep -n ReadContents 3af5ddae -- src/votv-coop returns exactly two lines: the definition at container_contents_sync.cpp:271 and the call at :334 (BroadcastContainer); both are inside the file's `namespace {` opened at :32.
- [SOURCE_PROVEN] A protocol bump is safe upstream because mismatched peers are refused before joining: the session peeks the peer's version on an unparsed header and retires the pending connection with a named reason, kProtocolVersion is bound into the admission preimage, and the browser join gate refuses a differing advertised proto.
    evidence: 3af5ddae:src/votv-coop/src/coop/net/session.cpp:262-267; peer_admission_internal.h:57-58; session_manager.cpp:592. kProtocolVersion is 150 / 151 / 152 at ba6d8c39 / f690c017 / 3af5ddae.
- [SOURCE_PROVEN] If a wire custody transfer is ever needed, it must reuse ReliableKind::ContainerContents as a new op byte, not a field on PropDropIntent and not a new kind: the blob lane already chunks at 220 bytes with a 56,100-byte ceiling and the shipped parser already refuses an unknown op, so an old peer degrades to a silent no-op rather than a corrupt apply.
    evidence: protocol.h:589, :1367-1374 (BlobChunkPayload data[220]); blob_chunks.h:45-47 (MaxBlobBytes = 220*255); container_contents_sync.cpp:563 (`op != kOpContents` -> Ingest::Handled).
- [MEASURED] The R3Q arms remain usable despite the packet's NOT_CLEAN verdict: the seven findings are in the tool-policy and cleanup aggregates and in non-load-bearing record defects, and no gameplay-arm rerun is required.
    evidence: R3Q/LANES/ws-runtime-review-codex/VERDICT.txt (CONTROL_PASS, EXACT_REPRODUCTION=TRUE, RUNTIME_RERUN_REQUIRED=none) and STATUS.json findings F-01..F-07; REVIEW.md 'The primary arm exactly reproduces...' and 'The control arm preserves the exact required Drive content...'.
- [MEASURED] The two R3Q arms vary the acting peer for the whole grab/inventory/place chain, so they do not isolate which transition loses the content, and there is one run per route.
    evidence: R3Q REVIEW.md, primary-arm CLAIM block: 'GAP The two arms vary the acting peer for the entire grab/inventory/place chain; they do not isolate which transition loses the content. There is one run per route.'
- [MEASURED] The persistence result is bounded: NOT_RECOVERABLE_AFTER_RELOAD; ABSENT_WITHIN_SEARCHED_SCOPE, with PERMANENT_DATA_LOSS = ABSENT_WITHIN_DECLARED_SEARCHED_SCOPE; UNKNOWN_BEYOND_SCOPE over four declared surfaces only.
    evidence: R3Q/PACKET2/r3q-runtime-packet-2/RESULT.json PERSISTENCE_RESULT, PERMANENT_DATA_LOSS and DECLARED_SEARCHED_SCOPE; REVIEW.md persistence section (world hash 2CBFFC12... not seed CCCB6FCE..., key reminted as EID 4202, stores held Crowbar x1 and Hook x2 and no drive).
- [INFERRED] OBS-R3Q-C01 is not on the critical path of the client-authored primary chain and does not have to be fixed first, but it does block any client-side acceptance observable. The host-side guard for the client-place direction is ordering (the intent is authored a tick late); there is no equivalent for the host-place direction.
    evidence: prop_drop_intent.h:12-15 (the husk-destroy ordering argument) and the client drain at prop_drop_intent.cpp:263-363; the eid-0 sentinel written at prop_destroy_seam.cpp:131 and deliberately accepted at event_dispatch_entity.cpp:236-242; the client's key fallback that destroys the mirror at remote_prop_destroy.cpp:136-151. R3Q recorded exactly one drop intent and one host materialization in the primary arm.
- [SOURCE_PROVEN] BUG-017's own row in the reviewed applicability matrix is credited nothing (U1/U2/U3/U4); only MVF-0001 carries H-M under OPTION_2. The matrix was frozen before the R3Q runtime arms ran, so promoting the cell is a reviewer decision this design does not take.
    evidence: f3d6b59d:design/relay-adoption/BUG-APPLICABILITY-MATRIX.md:51 (BUG-017), :84 (MVF-0001 H-M), :87 (MVF-0004), cell key at :16-22.
- [SOURCE_PROVEN] docs/devices.md documents the container-contents lane in full but its ownership and wire-message tables have no row for it, so any patch here owes both rows plus a custody row.
    evidence: 3af5ddae:docs/devices.md:41-71 (the model) versus :171-183 ('Who owns what', no container-contents row) and :185-196 ('Wire messages', no ContainerContents row).
- [MEASURED] The main repository was read-only throughout: no checkout, no branch switch, no commit, no worktree.
    evidence: git -C S:/GAMEMODDING/Multivoid-Private status --porcelain returns only the pre-existing '?? .r3q-bootstrap/'; git rev-parse --abbrev-ref HEAD returns private/dev. All source reads used git show / git diff / git grep against refs.

# RECOMMENDATIONS
- (medium) Send BUG017-CONTRACT-DESIGN-DRAFT.md to an independent design review before any implementation, and direct the reviewer at section 9's five attack points first — chiefly the nested-index neuter asymmetry (a naive reuse of ReadContents for the host-local capture would ship a NEW data loss) and the fresh-slot-empty refusal (without it the fix double-applies on the host-authored path that currently works).
    why: MVF-0004's exact handoff requires INDEPENDENT_REVIEW_REQUIRED=yes for the design, and DECISION.md's DO-NOT-WAIT clause admits a small isolated fix only when a diff-scoped review proves it does not conflict with the ten-axis direction. The two named defects are both silent-data-loss shaped.
    depends_on: the draft as written
- (small) Adopt the host-local custody park as the primary mechanism and hold the kOpCustody wire op as a separate, later, separately-reviewed increment.
    why: It closes the measured chain with zero wire bytes, so the same patch text serves upstream and the PRIVATE b150 line (which must not change the wire); it reuses shipped reviewed primitives; and it keeps the patch to two production files at three lines each. A wire op is only justified if Q-3 shows a chain where the host never held the contents.
    depends_on: design review
- (medium) Author the standalone test harness FIRST and red — tests/custody_park/{run.ps1,qualification.cpp,stubs/} plus candidate_17_container_custody.cpp — against a pinned pre-fix predicate, in the 1c393401 baseline_delivery and 15589b7f candidate_XX styles, and require the ten omission cases listed in section 4.2.
    why: Doctrine Step 8 requires every gate shown red before it is trusted, and the programme's standing self-inflicted defect class is a probe narrower than the claim it supports. Test 2 (DiscardedFreshNotEmpty) and test 11 (the neuter asymmetry) are the two assertions that turn section 9's attack points into machine-checked facts.
    depends_on: design review; a decision to proceed to implementation
- (medium) Run rig arm A2 (host-authored CONTROL on the fixed build) BEFORE arm A1 (the primary), and score A1-A3 and A5 on host observables only. Cite the existing R3Q A0 result rather than re-running the unfixed arm.
    why: A fix that silently breaks the currently-working host-authored path is worse than the bug, so the regression arm must gate the success arm. R3Q's RUNTIME_RERUN_REQUIRED=none stands for the unfixed build, so re-running A0 spends the rig for nothing.
    depends_on: a built fixed DLL and separate rig authorisation
- (small) Fix OBS-R3Q-C01 (the client's keyed eid-0 destroy killing a just-placed mirror) in its own small, separately-reviewed patch, sequenced before rig arm A4 and before any player-facing release note about BUG-017.
    why: C01 does not block this fix and must not be merged into it (one patch, one defect), but it does block every client-side acceptance observable, and shipping restored contents into a box whose mirror the client loses is a worse user-visible outcome than the empty box.
    depends_on: nothing; independent of this design
- (medium) Obtain authorisation for the bounded full-stack GObjStack probe MVF-0004 specifies (record {key, index, stack length, ordered hash} before the take, after the host destroy, after materialization, and after save/reload) to settle Q-2 and Q-4.
    why: It converts the orphan-slot reading from INFERRED to measured, decides between the recommended write-into-the-fresh-slot strategy and the smaller re-install-parkedIndex alternative, and would let section 1.4 step 5 be stated at its true strength instead of resting on an in-tree bytecode comment.
    depends_on: operator rig authorisation; not required for the recommended strategy
- (small) Have the reviewer rule explicitly on Q-1 — whether the R3Q arms promote BUG-017's applicability cell from U2 — and record the ruling in the ledger rather than letting this design's existence imply the promotion.
    why: The matrix was frozen before the arms ran and credits BUG-017 nothing. Silent promotion by a downstream document is exactly the over-strength pattern the R3P architecture review closed eight cells for.
    depends_on: design review

# GAPS
- Which single transition loses the content is UNKNOWN. The R3Q control and primary arms vary the acting peer across the whole grab/inventory/place chain together, and there is one run per route, so no per-transition cause and no frequency can be claimed. Section 7 Q-6 gives the three single-transition arms that would settle it; the fix does not need the answer because it covers the whole custody window, but no causal wording may be published without it.
- Whether the host's old populated GObjStack slot survives the container's destruction (orphaned) or is cleared/reclaimed by the native teardown is UNKNOWN. The coop layer provably does not clear it, but the Blueprint/native side was not read and the bounded full-stack probe was not authorised or run.
- Whether propInventory_C::init allocates a fresh slot, reuses a free one, or reads ints[0][0] when loadData never ran is UNKNOWN. The design's narrative rests on an in-tree bytecode-derived comment (container_contents_sync.cpp:252-257) that this task did not re-measure.
- Whether the capture point in DestroyResolvedLocalActor_ can safely read GObjStack[Index].obj after Unpin and before K2_DestroyActor is UNKNOWN (Q-5). A fallback exists at the post-native destroy seam, where prop_lifecycle.cpp:280-283 states reads on the actor are still valid, but that would change the design's shape.
- Whether a wire custody transfer is needed at all is UNKNOWN (Q-3): no arm has yet tested a container whose contents the host never held (a client filling a carried container, or a client-born container via the ReelEjectIntent / container-extract birth path).
- Everything outside R3Q's four declared searched surfaces is UNKNOWN. The persistence result is bounded absence, not unrestricted permanent data loss, and this design must not be summarised as the latter.
- No build was configured, no game process was started, no rig arm was run, and no probe was executed in this task. Every acceptance test in section 4 is a proposal whose detector has not yet been shown returning TRUE on a known-true input.
- The R3Q packet's own verdict is NOT_CLEAN, and GATE-POLICY-FINAL-BYTES and GATE-CLEANUP-SNAPSHOT are independently UNKNOWN rather than PASS. The gameplay arm outcomes are upheld, but a reviewer wanting an unrestricted tool-use negative or an all-class cleanup negative behind them does not have one.
- No paired-peer log analysis was done for the fixed build (none exists), and the player-log crossref that links candidate 8 (place observations dropped at a 32-entry queue cap) to BUG-017 is explicitly an UNREVIEWED ORCHESTRATOR CORRELATION, not evidence. If that cap is also implicated, this design does not address it — the cap is prop_drop_intent.cpp:141-144, kMaxPending = 32.
