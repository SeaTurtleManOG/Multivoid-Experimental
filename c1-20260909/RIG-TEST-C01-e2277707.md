# RIG TEST — OBS-R3Q-C01 emitter gate on the b150 fixes build (e2277707)

Status: PREPARED, NOT RUN, NOT DEPLOYED. The operator said on 2026-09-09 they are not ready for manual work; do not deploy or start anything until they say so. Needs the operator; one rig owner. Do not start until the drills on this
build have exit 0 (log `C1\EVIDENCE\drills-b150-aligned-fixes-e2277707.log`).

## Build under test
```
BRANCH      private/b150-aligned-fixes @ e22777072ca89d4b604e0b3218044a03b8955cba
DLL         S:\GAMEMODDING\Multivoid-Private-B150\build\votv-coop-b150\Release\main.dll
SHA256      58d0d365fc99eb1611c67f3487dfb07ee726efdf61fb7ba74b96b50afd528b89
EMBEDS      MULTIVOID_SOURCE_COMMIT=e2277707...   (verified by byte search)
CONTAINS    the four reviewed fixes + the C01 host emitter gate + candidate 8 (all reviewed CLEAN)
DEPLOY TO   Game_0.9.0n_HOST and Game_0.9.0n_CLIENT_1 ...\Win64\Mods\Multivoid\dlls\main.dll
            (both currently hold the official b150 bytes with no embedded commit; copy, then
            recompute sha256 on both destinations and paste before launching)
LAUNCH      S:\GAMEMODDING\Multivoid-B150-Evidence\rig_mp_b150.py (untracked wrapper; -saveddirsuffix
            per role); cold boot needs --boot-timeout 300; VOTVCOOP_FRESH=1 for a blank world.
NEVER       contact the official master server; Direct/private route only (policy selftest 3/3).
```

## Arm
Re-run R3Q-CONTROL-01 exactly as written in
`S:\GAMEMODDING\Multivoid-B150-Evidence\public-r3q-20260904T0026Z\OPERATOR-CHECKLIST-01-CONTROL.md`
(HOST performs C0..C5 on the Drive box; CLIENT does nothing to the box). Same seed, same key
`HlwQ1k8xmW4zxWDHX-VZFw` if the seed is reused; otherwise record the placement key from the host log.

## Scoring — three paired log assertions (from the C01 design; all three must hold)
| # | Peer | Assertion |
|---|---|---|
| a | HOST | the new suppression WARN (`HOST suppressed key-only DESTROY ... key=<placement key>`) is present at C4, AND there are ZERO `grab_hook[destroy-seam]: HOST broadcasting DESTROY ... key='<placement key>' eid=0` lines for it |
| b | CLIENT | `sync::CreateOrAdoptPropMirror: eid=<E> bound to actor=<A>` for the placement key at C4 with NO later `remote_prop::OnDestroy: key '<K>' eid=0 -> destroying local actor <A>` and no `PropPose key '<K>' eid=<E> -- no local match` / SUSTAINED-unresolved-pose line for the rest of the arm |
| c | CONTROL | the host's C2/C5a pickups still emit a NONZERO-eid DESTROY and the client still logs `OnDestroy: key '<K>' eid=<nonzero> -> destroying local actor` — mirrors are still torn down (falsifier for a fix that merely disabled the destroy lane) |

Visual: the C4/C5 paired captures show the box on the CLIENT's floor where the 2026-09-04 run showed
an empty floor. The drive must still be present at C5 on the host (this is the BUG-017 control chain;
it PASSED before and must still pass — a change here is a regression finding).

## Also observable in the same session (no extra steps)
- The four retargeted fixes' log signatures: no `ParamFrame::Set: unknown param` storm; no
  `PropSpawn elementId=0x00000000 ... dropping`; no `drive_sync: pending apply ... expired` for a
  resolvable actor; `connect-snapshot -- sent N` accounting now truthful.
- Candidate 8: `[PROP-DROP] client pending-place cap` lines, if any, now name what was evicted.

## Abort conditions
| condition | say |
|---|---|
| nicknames/windows not as the checklist states | `ABORT wrong window identity` |
| two candidate Drive boxes | `ABORT two candidate boxes` |
| either peer's boot banner does not read `Multivoid 0.9.0n b150` with the C1 DLL sha | `ABORT wrong build` |
| a VotV process this run did not start is running | `ABORT foreign game process` |
